---
es:
  activerecord:
    errors:
      models:
        twixy:
          attributes:
            username:
              bad_format: 'debe empezar con una letra y solo puede contener letras del alfabeto inglés, números o los siguientes símbolos: %{symbols}'
  errors:
    messages:
      format: 'debe tener uno de los siguientes formatos: %{formats}'
      oversize: tiene que tener un tamaño menor %{size}
  site_name: Twixo
